server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DavidRomeroN/DAD-0501.git
          searchPaths: config-data
          default-label: registry-server
  security:
    user:
      name: root
      password: 123456